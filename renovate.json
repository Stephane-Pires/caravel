{
    "$schema": "https://docs.renovatebot.com/renovate-schema.json",
    "extends": ["config:base"],
    "commitMessagePrefix": "chore(deps): :arrow_up:",
    "commitMessageTopic": "{{depName}}",
    "commitMessageExtra": "from {{ccurrentVersion}} to v{{newVersion}}"
}
---
"caravel": patch
---

Add healthcheck to db inside docker-compose in order to launch the application once the database is up. Also update the path for the volume of postgres due to a change in postgres 18

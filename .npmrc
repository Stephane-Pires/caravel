# Use copy mode instead of hardlinks (required for Docker bind mounts on macOS)
package-import-method=copy

# Store pnpm cache inside the container's native filesystem (not on bind mount)
store-dir=/home/node/.pnpm-store
